function startLayer2Training(){if(l2Running)return;resetLayer2(true);l2Running=true;buildStatusGrid('l2StatusGrid',L2_MODELS);document.getElementById('btnL2Start').disabled=true;document.getElementById('btnL2Pause').disabled=false;L2_MODELS.forEach(runModelL2)}
function runModelL2(m){let tick=0,id=setInterval(()=>{if(l2Paused)return;tick++;let pct=Math.min(100,Math.round(tick/m.totalTicks*100)),loss=m.genLoss(tick),last=loss[loss.length-1];document.getElementById('bar-'+m.id).style.width=pct+'%';document.getElementById('ep-'+m.id).textContent=tick+'/'+m.totalTicks;if(last)document.getElementById('loss-'+m.id).textContent=last.val.toFixed(4);if(tick>=m.totalTicks){clearInterval(id);let c=document.getElementById('sc-'+m.id);c.classList.add('done');document.getElementById('bar-'+m.id).style.width='100%';document.getElementById('ep-'+m.id).textContent='Done';document.getElementById('st-'+m.id).textContent='✓ Complete';document.getElementById('st-'+m.id).className='s-status done';checkL2Complete()}},Math.max(30,Math.round(m.realSec*1000/simSpeed/m.totalTicks)));l2Intervals.push(id)}
function checkL2Complete(){if(L2_MODELS.every(m=>document.getElementById('sc-'+m.id).classList.contains('done'))){l2Running=false;document.getElementById('btnL2Start').disabled=false;document.getElementById('btnL2Pause').disabled=true;populateL2Results();initL2ResultCharts();showToast('Layer 2 training complete')}}
function togglePauseL2(){l2Paused=!l2Paused;document.getElementById('btnL2Pause').textContent=l2Paused?'▶ RESUME':'⏸ PAUSE'}
function resetLayer2(silent){l2Intervals.forEach(clearInterval);l2Intervals=[];l2Running=false;l2Paused=false;document.getElementById('btnL2Start').disabled=false;document.getElementById('btnL2Pause').disabled=true;if(!silent)document.getElementById('l2StatusGrid').innerHTML=''}
function populateL2Results(){let rows=[['Persistence',0.5673,'—','—','—',0,'—','—','0.0'],['XGBoost Baseline',0.1152,0.1684,0.119,0.1081,0.797,0.894,22,'48.2'],['XGBoost_improved',0.1062,0.1558,0.1104,0.0996,0.813,0.919,17,'98.4'],['LSTM Baseline',0.1057,0.1583,0.1088,0.1016,0.814,0.91,19,'262.0'],['LSTM_improved',0.0984,0.1487,0.1009,0.0942,0.827,0.972,6,'485.3']];document.querySelector('#tblL2 tbody').innerHTML=rows.map(r=>'<tr>'+r.map((x,i)=>`<td>${i===0?'<strong>'+x+'</strong>':x}</td>`).join('')+'</tr>').join('')}
function initL2ResultCharts(){new Chart(document.getElementById('chartL2NRMSE'),{type:'bar',data:{labels:['XGB Base','XGB Improved','LSTM Base','LSTM Improved'],datasets:[{label:'NRMSE',data:[.1152,.1062,.1057,.0984],backgroundColor:[C.green,C.green,C.cyan,C.cyan]}]}});new Chart(document.getElementById('chartL2Delta'),{type:'bar',data:{labels:['XGBoost','LSTM'],datasets:[{label:'Relative Improvement %',data:[7.8,6.9],backgroundColor:[C.green,C.cyan]}]}});new Chart(document.getElementById('chartL2Radar'),{type:'radar',data:{labels:['1 min','5 min','15 min','30 min'],datasets:[{label:'XGB imp',data:[.100,.104,.108,.111],borderColor:C.green},{label:'LSTM imp',data:[.091,.095,.100,.103],borderColor:C.cyan}]}});new Chart(document.getElementById('chartL2Warnings'),{type:'bar',data:{labels:['XGB 15m','LSTM 15m','XGB 30m','LSTM 30m'],datasets:[{label:'High Temp Recall',data:[.919,.972,.901,.96],backgroundColor:[C.green,C.cyan,C.green,C.cyan]}]}});for(const [id,col,spread] of [['chartL2ScatterXGB',C.green,.015],['chartL2ScatterLSTM',C.cyan,.009]])new Chart(document.getElementById(id),{type:'scatter',data:{datasets:[{label:id.includes('LSTM')?'LSTM imp':'XGB imp',data:Array.from({length:100},()=>{let x=.05+Math.random()*.15;return{x,y:x+(Math.random()-.5)*spread}}),backgroundColor:col}]}});new Chart(document.getElementById('chartL2Attn'),{type:'bar',data:{labels:Array.from({length:30},(_,i)=>i+1),datasets:[{label:'Attention',data:Array.from({length:30},(_,i)=>.02+.025*Math.sin(Math.PI*i/29),backgroundColor:C.cyan}]}})}
